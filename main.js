const adjectives = ['best', 'smartest', 'sweetest', 'most awesome'];
const nomen = ['coder', 'programmer', 'hacker'];

function shuffleWords(adjectivesLength, nomenLength) {
    const randomAdjectiveIndex = Math.floor(Math.random() * 4);
    const randomNomenIndex = Math.floor(Math.random() * 3);
    return [randomAdjectiveIndex, randomAdjectiveIndex];